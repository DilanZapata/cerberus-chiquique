node_modules
**/node_modules
dist
**/dist
.next
**/.next
.expo
**/.expo
.expo-shared
coverage
**/coverage
*.log

.git
.claude
docs

# App movil no participa en las imagenes de api/web, PERO su package.json si
# tiene que estar presente (los Dockerfiles lo copian) porque npm necesita el
# package.json de CADA workspace declarado en la raiz para resolver
# package-lock.json, aunque no instalemos sus dependencias.
apps/mobile/*
!apps/mobile/package.json

# Datos de runtime, nunca deben entrar al build
apps/api/uploads
apps/api/generated

.env
.env.local
.env.*.local
docker-compose.local-test.yml
